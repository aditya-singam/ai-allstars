{
  "name": "ai-allstars",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm ci --omit=dev && npm run build",
    "start": "cd server && npm ci --omit=dev && npm start",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
} 